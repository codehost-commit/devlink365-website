/**
 * DevLink365 — share your localhost with anyone.
 *
 * The CLI is the product; this module is the same thing with a programmatic
 * surface, for scripts and CI that want a public URL for a few minutes.
 *
 * ```ts
 * import { createTunnel } from "devlink365";
 *
 * const tunnel = await createTunnel({ port: 3000, ttlMs: 60 * 60 * 1000 });
 * console.log(tunnel.details?.url);
 * // ... later
 * tunnel.stop();
 * ```
 */
export { Tunnel, createTunnel, DEFAULT_RELAY, DEFAULT_TTL_MS, MAX_TTL_MS, VERSION, } from "./tunnel.js";
export { DevLinkError, isDevLinkError } from "./core/errors.js";
export { detectLocalServer, isPortOpen, probeHttp, validatePort, COMMON_PORTS, } from "./core/detect.js";
export { createRelay } from "./relay/server.js";
export { TunnelRegistry } from "./relay/registry.js";
export { PROTOCOL_VERSION, AGENT_PATH } from "./core/protocol.js";
export { normalizeLabel, randomId, subdomainFor } from "./core/ids.js";
//# sourceMappingURL=index.js.map