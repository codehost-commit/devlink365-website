/**
 * The pages a visitor sees when a link does not work.
 *
 * These are the only DevLink365 surface most visitors will ever see, so they
 * get the same care as the marketing site: no stack traces, no blame, and a
 * plain sentence explaining that the link was temporary by design.
 */
export interface StatusPageOptions {
    status: number;
    eyebrow: string;
    title: string;
    body: string;
    detail?: string;
}
export declare function statusPage(options: StatusPageOptions): string;
export declare const PAGES: {
    readonly unknown: (host: string) => string;
    readonly expired: (host: string) => string;
    readonly offline: (host: string) => string;
    readonly full: () => string;
    readonly throttled: (retryAfterSeconds: number) => string;
    readonly tooLarge: () => string;
    readonly apex: () => string;
};
//# sourceMappingURL=pages.d.ts.map