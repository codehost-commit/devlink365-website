/**
 * Every error a developer can hit while using the CLI answers three questions:
 * what happened, why it happened, and what to do next. Stack traces are for
 * bugs in DevLink365 — not for "you forgot to start your dev server".
 */
export interface DevLinkErrorInit {
    code: string;
    /** What happened, in one line. */
    message: string;
    /** Why it happened, when we can say honestly. */
    because?: string;
    /** Concrete next steps, most likely fix first. */
    next?: string[];
    cause?: unknown;
}
export declare class DevLinkError extends Error {
    readonly code: string;
    readonly because: string | undefined;
    readonly next: string[];
    constructor(init: DevLinkErrorInit);
}
export declare function isDevLinkError(value: unknown): value is DevLinkError;
/** Turns a low-level socket failure into something a person can act on. */
export declare function explainConnectionFailure(err: unknown, relayUrl: string): DevLinkError;
//# sourceMappingURL=errors.d.ts.map