/**
 * Terminal output.
 *
 * The CLI prints one thing that matters — the public URL — and everything else
 * stays quiet enough not to compete with it. Colour is opt-out (NO_COLOR, a
 * non-TTY stdout, CI) and every symbol has an ASCII fallback for terminals that
 * cannot render box drawing.
 */
export declare const colorEnabled: boolean;
export declare const style: {
    bold: (text: string) => string;
    dim: (text: string) => string;
    underline: (text: string) => string;
    red: (text: string) => string;
    green: (text: string) => string;
    yellow: (text: string) => string;
    blue: (text: string) => string;
    magenta: (text: string) => string;
    cyan: (text: string) => string;
    gray: (text: string) => string;
    greenBright: (text: string) => string;
};
export declare const symbol: {
    tick: string;
    cross: string;
    arrow: string;
    down: string;
    dot: string;
    warn: string;
};
export declare function write(line?: string): void;
export declare function writeError(line?: string): void;
/** "4 hours", "45 minutes", "2 hours 30 minutes" — never "3.9999h". */
export declare function formatDuration(ms: number): string;
export declare function formatBytes(bytes: number): string;
export declare function statusColor(status: number): (text: string) => string;
export interface RunningBanner {
    local: string;
    publicUrl: string;
    expiresInMs: number;
    relayHost: string;
}
/** The block printed once, immediately after the tunnel opens. */
export declare function printRunning(banner: RunningBanner): void;
/** One line per proxied request, printed as they complete. */
export declare function printRequest(input: {
    method: string;
    url: string;
    status: number;
    durationMs: number;
    bytes: number;
}): void;
export declare function printNotice(message: string): void;
export declare function printGoodbye(requests: number, reason: string): void;
export interface RenderableError {
    code: string;
    message: string;
    because?: string | undefined;
    next?: string[];
}
/** The one place errors are formatted. What happened, why, what to do next. */
export declare function printError(error: RenderableError): void;
/** A quiet single-line spinner used while we look for the local server. */
export declare class Spinner {
    private text;
    private frames;
    private index;
    private timer;
    private active;
    constructor(text: string);
    start(): this;
    update(text: string): void;
    stop(): void;
}
//# sourceMappingURL=ui.d.ts.map