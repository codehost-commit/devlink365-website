#!/usr/bin/env node
/**
 * `npx devlink365`
 *
 * One command. Find the dev server, open a tunnel, print the link, get out of
 * the way. Everything here exists to keep that path short — flags are for the
 * cases where guessing would be wrong, never for the common case.
 */
interface CliOptions {
    port: number | undefined;
    host: string;
    ttlMs: number;
    label: string | undefined;
    relay: string;
    quiet: boolean;
    json: boolean;
    rewriteHost: boolean;
    insecure: boolean;
    yes: boolean;
}
/** Accepts 30m, 4h, 1h30m, or a bare number of hours. */
export declare function parseDuration(input: string): number;
export declare function parseArgs(argv: string[]): CliOptions | "help" | "version";
export declare function run(): void;
export {};
//# sourceMappingURL=cli.d.ts.map